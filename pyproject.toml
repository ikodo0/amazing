[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "mazegen"
version = "1.0.0"
dependencies = ["pydantic"]

[tool.setuptools.packages.find]
where = ["src"]